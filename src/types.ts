export interface PostsEntity {
  post_id: number;
  post_title: string;
  post_likes: number;
  post_user: string;
}
export interface UserEntity {
  user_id: number;
  username: string;
  email: number;
}
